# 公式与术语速查

## 1. 结果集合与近似

真实可达行为/状态集合为 `R`，分析结果代表的集合为 `A`：

- **过近似**：\(R\subseteq A\)。不漏真实行为，可能加入伪行为；
- **欠近似**：\(A\subseteq R\)。报告的行为有真实见证，可能漏掉行为；
- **精确**：\(A=R\)。

对坏状态集合 `Bad`：

- 过近似且 \(A\cap Bad=\varnothing\) ⇒ 可证明没有真实坏状态；
- 欠近似且 \(A\cap Bad\ne\varnothing\) ⇒ 可给出真实坏状态见证；
- 过近似“发现坏状态”可能误报；欠近似“未发现”不能证明安全。

### Soundness / Completeness

这两个词必须带上**查询方向**：

- 抽象解释常说 sound：抽象语义覆盖全部具体语义；
- 逻辑证明常说 sound：凡被证明的命题都是真的；
- 漏洞检测把“有漏洞”当正例时，又会用 TP/FP/FN 描述报告。

所以不要背“sound=无误报”或“sound=无漏报”的脱离语境口号，应直接写集合包含关系和正例。

### May / Must

- **may fact**：至少一条相关路径成立（存在量词）；
- **must fact**：所有相关路径成立（全称量词）。

may/must 是路径量词；over/under 是语义集合的近似方向；flow/context/path sensitivity 是区分状态的精度维度。三者不是同义词。真实 may 性质使用存在量词，但过近似分析报告的 may 候选可能来自不可行路径或抽象合并，不自动携带真实执行见证。

## 2. CFG 与 IR

控制流图：

\[
G=(N,E,entry,exit)
\]

- `N`：语句或基本块；
- `(u,v)∈E`：控制可能从 `u` 转移到 `v`；
- `pred(n)` / `succ(n)`：前驱/后继。

基本块 leader 常见规则：

1. 过程第一条指令；
2. 跳转目标；
3. 跳转指令紧后的指令。

SSA 性质：每个标量版本只定义一次。汇合点：

\[
x_3=\phi(B_1:x_1,B_2:x_2)
\]

`φ` 根据控制流实际来自哪个前驱取对应值。SSA 仍依赖 CFG；普通寄存器 SSA 不自动刻画内存别名。

## 3. 通用数据流框架

为节点 `n` 维护：

- `IN[n]`：执行前抽象状态；
- `OUT[n]`：执行后抽象状态。

典型 GEN/KILL 转移：

\[
OUT[n]=GEN[n]\cup(IN[n]\setminus KILL[n])
\]

前向分析：

\[
IN[n]=\mathop{meet}_{p\in pred(n)}OUT[p]
\]

后向分析：

\[
OUT[n]=\mathop{meet}_{s\in succ(n)}IN[s]
\]

### 三个经典分析

| 分析 | 方向 | 性质 | 汇聚 | 转移 |
|---|---|---|---|---|
| Available Expressions | forward | must | 交 | `GEN ∪ (IN−KILL)` |
| Reaching Definitions | forward | may | 并 | `GEN ∪ (IN−KILL)` |
| Live Variables | backward | may | 并 | `USE ∪ (OUT−DEF)` |

#### 可用表达式

\[
IN[n]=\bigcap_{p\in pred(n)}OUT[p]
\]

对 `x=rhs`：

\[
KILL[n]=\{e\mid x\in operands(e)\}
\]

\[
GEN[n]=EvalExpr(rhs)\setminus KILL[n]
\]

边界入口为空；其余初值通常为候选表达式全集。

#### 到达定义

\[
IN[n]=\bigcup_{p\in pred(n)}OUT[p]
\]

直接赋值 `x=...` 生成当前位置的定义，并杀死其他对 `x` 的定义。间接写只有在能证明唯一 must-target 时才可强更新；对多个 may-target 必须弱更新并保留旧定义。入口和其余点通常从空集迭代。

#### 活跃变量

\[
OUT[n]=\bigcup_{s\in succ(n)}IN[s]
\]

\[
IN[n]=USE[n]\cup(OUT[n]\setminus DEF[n])
\]

出口边界为空；其余点从空集迭代。

### Worklist 模板

```text
initialize boundary and all node states
worklist = all reachable nodes
mark every node as queued

while worklist not empty:
    n = pop(worklist)
    mark n as not queued
    old = propagated_state[n]
    recompute meet and transfer for n
    if propagated_state[n] != old:
        enqueue every dependent(n) not already queued
```

也可以不把所有节点一次性入队，但必须维护“未处理”状态，保证每个可达节点至少执行一次转移函数。

- forward 的依赖者通常是后继；
- backward 的依赖者通常是前驱；
- 有限高度格 + 单调转移 + 正确格端初值与单向更新 + 公平调度 ⇒ 终止；
- 无限高度域可能需要 widening。

## 4. 过程间分析

ICFG 常见边：

- 过程内 CFG 边；
- call edge：call site → callee entry；
- return edge：callee exit → 对应 return site；
- call-to-return edge：按分析语义概括调用影响。

可实现路径要求调用/返回匹配。配置状态可写为：

\[
\langle n,S\rangle
\]

其中 `n` 为 ICFG 节点，`S` 为调用栈：call 压栈，匹配 return 才弹栈。

`k`-call-string 上下文只保留最近 `k` 个调用点：

- `k=0` 表示上下文不敏感；
- `k` 越大，通常区分的调用上下文越多，时间和空间开销也越大；
- 截断后的 call string 不是完整运行栈，返回时应通过 continuation 映射回相应 caller；若显式模拟有限栈，则需使用 overflow/unknown-prefix 等保守返回语义；
- 该方法与经典 k-CFA 的上下文截断思想相关，但不应无条件视为同一算法；
- 对象敏感分析可用 receiver 分配点序列表示上下文。

## 5. 指针与别名

程序点 `ℓ` 处指针 `p` 的 points-to 集：

\[
pts_\ell(p)\subseteq Obj
\]

- **分析层面的 may-alias**：若 \(pts(p)\cap pts(q)\ne\varnothing\)，分析器通常报告 may-alias，但过近似结果可能是假阳性，不保证存在真实别名执行；
- **must-alias**：要求在所有相关执行中二者指向同一具体位置。\(pts(p)=pts(q)=\{o\}\) 只有在 \(o\) 代表唯一具体位置、两者同时有效且已排除 null、生命周期和汇总对象歧义时，才是充分条件；
- **no-alias**：若 sound 的 may-points-to 结果满足 \(pts(p)\cap pts(q)=\varnothing\)，则可排除内存别名。

### Andersen 包含式约束

| 语句 | 约束 |
|---|---|
| `p=&o` | \(\{o\}\subseteq pts(p)\) |
| `q=p` | \(pts(p)\subseteq pts(q)\) |
| `q=*p` | \(\forall o\in pts(p):pts(o)\subseteq pts(q)\) |
| `*p=q` | \(\forall o\in pts(p):pts(q)\subseteq pts(o)\) |
| `q=&p->f` | \(\forall o\in pts(p):\{o.f\}\subseteq pts(q)\) |

基础 Andersen 通常是 inclusion-based、flow-insensitive、context-insensitive。字段敏感 GEP 是扩展维度，不应把所有实现都说成相同精度。

### 敏感性

- **flow-sensitive**：区分同一过程不同程序点；
- **path-sensitive**：区分不同分支条件组合；
- **context-sensitive**：区分同一函数的不同调用上下文；
- **field-sensitive**：区分对象不同字段；
- **object-sensitive**：用 receiver/分配对象区分面向对象调用上下文。

“敏感”通常提高精度，但不自动意味着 sound；“不敏感”也不自动意味着 unsound。

## 6. 抽象解释

### 抽象域与 Galois 连接

- 具体域：\((C,\sqsubseteq_C)\)；
- 抽象域：\((A,\sqsubseteq_A)\)；
- 抽象函数：\(\alpha:C\to A\)；
- 具体化函数：\(\gamma:A\to C\)。

Galois 连接：

\[
\alpha(c)\sqsubseteq_A a\quad\Longleftrightarrow\quad c\sqsubseteq_C\gamma(a)
\]

设 \(f:C\to C\)、\(f^\#:A\to A\)。抽象转移 \(f^\#\) 的 sound 条件为：

\[
f(\gamma(a))\sqsubseteq_C\gamma(f^\#(a)).
\]

由 Galois 连接，等价地：

\[
\alpha(f(\gamma(a)))\sqsubseteq_A f^\#(a).
\]

若具体域明确取 \(C=\mathcal P(\Sigma)\)，且 \(\sqsubseteq_C\) 就是集合包含，则第一式也可写成 `⊆`。

### 偏序和格

偏序满足：自反、反对称、传递。

- join / 最小上界：\(x\sqcup y\)；
- meet / 最大下界：\(x\sqcap y\)；
- bottom：\(\bot\)；
- top：\(\top\)。

完备格要求任意子集都有 join 和 meet。幂集格：

\[
(\mathcal P(S),\subseteq,\cup,\cap,\varnothing,S)
\]

幂集格从 `∅` 到 `S` 的最长严格链有 `|S|` 条边、`|S|+1` 个元素；“格高度”采用哪种计数必须说明。

### 符号域

本仓库使用过两种不同的符号域：

1. 五点简化域
   \[
   \{\bot,\mathsf{Neg},\mathsf{Zero},\mathsf{Pos},\top\},
   \]
   它不包含“非负”“非正”等中间元素；
2. 完整符号幂集域
   \[
   \mathcal P(\{-,0,+\}),
   \]
   它允许 \(\{0,+\}\) 等组合。

在完整幂集域中：

\[
\{+\}\sqcup\{0\}=\{0,+\},\qquad
\{+\}\sqcap\{0\}=\varnothing.
\]

两种域都可使用，但不能混用其合法元素和运算表。

### 区间域

\[
I=
\{[l,h]\mid
l\in\mathbb Z\cup\{-\infty\},
\ h\in\mathbb Z\cup\{+\infty\},
\ l\le h\}
\cup\{\bot\}
\]

其中 \(\gamma(\bot)=\varnothing\)；普通区间只用允许的有限/单侧无穷端点，因此不会再产生与 \(\bot\) 重复的空元素。

包含序：

\[
[l_1,h_1]\sqsubseteq[l_2,h_2]
\iff l_2\le l_1\land h_1\le h_2
\]

join（凸包）：

\[
[l_1,h_1]\sqcup[l_2,h_2]
=[\min(l_1,l_2),\max(h_1,h_2)]
\]

meet（交）：

\[
[l_1,h_1]\sqcap[l_2,h_2]
=[\max(l_1,l_2),\min(h_1,h_2)]
\]

若下界大于上界则为 `⊥`。区间 join 不是一般集合并集：如 `[0,0]⊔[2,2]=[0,2]`，会包含 1。

### Widening

经典区间 widening：

\[
[l_1,h_1]\nabla[l_2,h_2]=[l_3,h_3]
\]

\[
l_3=\begin{cases}
-\infty,&l_2<l_1\\
l_1,&l_2\ge l_1
\end{cases}
\qquad
h_3=\begin{cases}
+\infty,&h_2>h_1\\
h_1,&h_2\le h_1
\end{cases}
\]

对底元素约定：

\[
\bot\nabla I=I,
\qquad
I\nabla\bot=I.
\]

第一次出现非底候选时也可直接用候选初始化，从下一次更新起再 widening。只在选定循环头 widening，常先做若干轮普通迭代；它保证/加速终止，但会损失精度。

### Narrowing

对 widening 得到的后不动点旧区间 `x=[l1,h1]`，以及满足 `y⊑x` 的下降候选 `y=[l2,h2]`：

\[
[l_1,h_1]\triangle[l_2,h_2]=[l_3,h_3]
\]

\[
l_3=\begin{cases}l_2,&l_1=-\infty\\l_1,&\text{otherwise}\end{cases}
\qquad
h_3=\begin{cases}h_2,&h_1=+\infty\\h_1,&\text{otherwise}\end{cases}
\]

对底元素约定：

\[
I\triangle\bot=\bot,
\qquad
\bot\triangle\bot=\bot.
\]

在新候选不大于旧值的前提下，\(\bot\triangle I\) 且 \(I\ne\bot\) 不是合法调用。Narrowing 的每轮结果还应保持“候选不大于结果、结果不大于旧值”，以维持后不动点下降；它能改善安全过近似，但不保证恢复最佳或精确结果，通常只执行有限轮。

## 7. YASA 术语

- **UAST**：Unified Abstract Syntax Tree，多语言统一 AST；核心结构统一，语言特性可放 `_meta`（课程叙述中常简称 meta），转换是有损抽象；
- **Analyzer**：符号解释/模拟执行组件，分基础、语言、框架层；
- **Symbolic Value**：分析中的抽象值或内存对象表示，不等同于具体运行值；
- **Scope**：变量/对象可访问空间；
- **State**：执行路径、调用栈、分支和变量等分析状态；
- **EntryPoint**：开始模拟执行的入口；可外部指定或由 Checker/框架规则采集；
- **Checkpoint**：Analyzer 在关键语义事件触发 Checker 的位置；
- **Checker**：事件驱动的可插拔分析组件；
- **CheckerManager**：注册 Checker 并在 checkpoint 依次调用；
- **CheckerPack**：一组 Checker ID 的组合；
- **rule config**：为 Checker 提供 Source/Sink/Sanitizer/EntryPoint 等用户规则；
- **Finding**：Checker 写入 ResultManager 的检测结果；
- **OutputStrategy**：把 findings 转换为 SARIF、JSON 等具体输出；
- **Source**：不可信/敏感数据起点；
- **Sink**：危险或敏感数据使用点；
- **Sanitizer**：验证、净化或阻断规则；
- **Taint Trace**：Source 到 Sink 的传播证据链；
- **UQL/AntQL**：课程演示的声明式查询入口；名称和可用接口可能随版本演进，实际使用必须核对固定版本。
