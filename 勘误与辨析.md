# 勘误与概念辨析

本文件列最容易影响后续学习的修正。逐条原文审计见各主笔记末尾：

- `notes/01-静态分析基础与中间表示.md`：“原文易误解或技术错误”；
- `notes/02-数据流分析.md`：“原文错误、歧义与纠正”；
- `notes/03-指针分析与抽象解释.md`：“原文易误解或技术错误”；
- `notes/04-YASA三次实验.md`：证据标签、版本警告与未知项。

## 1. 不可判定不等于不可分析

停机问题和 Rice 定理排除的是：对一般图灵完备程序的非平凡语义性质，同时要求**总终止、对所有输入有效、完全精确**的算法。它们不排除：

- 有限状态或受限语言上的精确分析；
- sound 过近似；
- 带真实见证的欠近似；
- 返回 unknown；
- 带预算的路径搜索；
- 人工不变量与交互证明。

“代码里是否出现字符串 `malloc`”是语法性质，不直接受 Rice 定理约束。内存泄漏、越界等性质也需先固定操作语义和量词，不能一句 Rice 定理带过。

## 2. 四组不能机械等同的概念

| 概念 | 回答的问题 | 不等于 |
|---|---|---|
| over / under approximation | 分析行为集合与真实行为集合如何包含 | may / must |
| may / must | 至少一条路径还是所有路径成立 | sound / complete |
| sound / complete | 在已定义查询方向下允许哪类错误 | 固定的“无误报/无漏报”口号 |
| sensitivity | 是否区分流、路径、上下文、字段、对象 | soundness |

尤其是：

- under-approximation 不是“must analysis”；
- flow-insensitive 分析可以 sound，只是通常较粗；
- path-sensitive 也可能因循环截断而漏行为；
- 在漏洞告警与安全证明中，“正例”可能相反，FP/FN 必须先定义。

## 3. 基础概念中的关键修正

1. 随机数例子的精确输出是 `yes` 或 `maybe`，不是 `yes/no`；
2. 动态执行提供真实轨迹，但 oracle、插桩和环境仍可能导致错误告警；
3. 符号执行不保证遍历所有路径；
4. `unknown`（信息不足）与 `undefined`（语言值/异常语义）不能混用；
5. “所有静态分析都遍历全部路径”不是定义。

## 4. IR 中的关键修正

1. CST 不只用于语法合法性，也用于格式保持重构、IDE 和增量编辑；
2. CFG 边表示直接转移，可达性是边的传递闭包；
3. 基本块“单入口、末尾离开”不代表只有一个后继；条件块可有两个；
4. 3AC 是“最多三个地址”，不是“只有三个元素”；
5. SSA 的 φ 依赖 CFG 前驱，不能取代 CFG；
6. 普通标量 SSA 不自动覆盖内存，仍需别名、内存提升或 Memory SSA；
7. LLVM 的 `getelementptr` 计算地址，不读取内存；读取由 `load` 完成。

## 5. 数据流分析中的关键修正

1. `a=a-1` 在标准 available-expressions 的语句出口处不能 GEN `a-1`：右侧用旧 `a`，出口处 `a` 已被定义；
2. AE 的候选表达式域必须先固定，条件节点究竟生成比较式还是算术子式不是自动决定；
3. 表达式“可用”还不等于可直接替换，必须有仍有效的保存值/值编号；
4. RD 可用“入口空集”或“入口伪定义”建模，但不能在一张表中混用；
5. 活跃变量结果只能在无副作用、无 volatile/原子/异常要求时支持删除死赋值；
6. Worklist 初始化必须保证每个可达节点至少求值一次（或采用等价的“未处理”机制）；之后在传播状态变化时调度依赖者；
7. 普通 ICFG 图可达会包含调用/返回不匹配的不可实现路径；需调用栈、call string 或 pushdown 方法；
8. 有限 `k` 截断若没有 continuation 或保守 overflow 返回语义，可能从粗糙变成欠近似；它也不能无条件等同于经典 k-CFA；
9. 真实 may 性质使用存在量词，但过近似分析报告的 may 候选可能来自不可行路径或上下文/对象合并，不自动提供真实见证；
10. may-RD 中的间接写若只有 may-target，必须弱更新并保留旧定义；只有唯一 must-target 才能强更新并 KILL。

## 6. 指针分析中的关键修正

1. pointer analysis、points-to analysis、alias analysis 相关但不严格同义；
2. points-to 对象还包括堆分配点、字段、数组区域和函数，不只是“被取地址变量”；
3. `pts(p)∩pts(q)≠∅` 只给 may-alias 候选，可能来自路径/上下文合并；
4. 两个单元素集合相同也未必 must-alias：分配点汇总对象可能代表多个动态实例；
5. 流敏感不自动允许强更新，汇总对象通常只能弱更新；
6. 基础 Andersen 常是 flow/context/path-insensitive，但字段敏感、上下文敏感等扩展存在；
7. Load/Store 在 points-to 新增时动态产生 Copy 约束；新增边必须传播源节点已有事实；
8. 指针分析提供内存关系，污点分析利用这些关系传播标签，二者不是同一分析。

## 7. 抽象解释中的关键修正

1. 抽象域不必有限；区间域有无限高度，因此才需要 widening；
2. `x⊔y` 是 `{x,y}` 的最小上界，不是“x 的最小上界与 y 的最小上界”；
3. 区间 join 是凸包，不是一般集合并；`[0,0]⊔[2,2]=[0,2]`；
4. `⊥` 通常表示不可达，不应直接拿来代表“已到达但变量未初始化”；应有独立 `Uninit` 值；
5. 课程所谓抽象轨迹实际更像“每个程序点的不变量映射”，不是一条具体路径；
6. 循环头的 CFG 前驱从一开始就包括入口边与回边，即使首轮回边状态为 `⊥`；
7. widening 的契约是有限稳定并覆盖候选，不只是“预测趋势”；
8. narrowing 只能恢复部分精度，不保证回到最小不动点；
9. “12 次”“5 次”取决于调度、计数、阈值和延迟策略，不是普遍复杂度定理；
10. 分析 C/LLVM 机器整数时还要建模溢出、`poison` 或未定义行为，不能默认数学整数；
11. 扩展整数区间应限制下界只允许 `-∞`、上界只允许 `+∞`，否则 `[±∞,±∞]` 会与 `⊥` 重复；widening/narrowing 还需显式定义 `⊥` 情形和下降前提。

## 8. YASA 资料中的版本与证据边界

1. “统一支持多语言”不等于“一条数据流天然跨多种语言运行时”；
2. UAST 是面向分析的有损统一，不保证能无损还原/重新编译；
3. “极大元选取”是官方设计准则，未公开机械求极大元的形式算法；
4. YASA 的分支模拟不等同于 SMT 驱动的完整路径可行性证明；
5. 有界循环/调用模拟是否 sound，取决于截断处是否有保守摘要；
6. JSON 字段、Checker hook、`info` 结构和命令参数会演进，实验必须固定版本；
7. `checker-config` 注册实现，`checker-pack-config` 组合 Checker，`rule_config` 为 Checker 提供规则数据，三者职责不同；
8. 课程明确展示 UQL，但公开资料不足以安全复原完整 grammar/标准库；不要用 CodeQL 语法猜造 YASA API；
9. 课程计划提到第四次 Web 框架适配，但官方合集和课程页只发布了三次实验；第三次视频确实用 Mux 适配演示 Checker 编写，完整 Mux/Django 文档仍只能作为补充资料，不能据此虚构第四课。

## 9. 一条通用审查规则

遇到任何分析结论，依次问：

1. 查询的正例是什么？
2. 具体语义和抽象域是什么？
3. 集合包含方向是什么？
4. 在哪些程序点、路径、上下文和对象上合并？
5. 循环/递归怎样终止？
6. 外部代码、反射、并发和语言未定义行为怎样处理？
7. 结论是课程原文、当前源码事实，还是教学推断？

能回答这七项，通常就能避免绝大多数术语和工程误解。
