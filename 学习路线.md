# 《静态程序分析原理与实践》学习路线

## 1. 总体主线

```text
为什么需要静态分析
  ↓
不可判定性与近似的代价
  ↓
程序表示：AST → CFG → 3AC/SSA
  ↓
格、单调性、不动点
  ↓
过程内数据流：AE / RD / LV
  ↓
调用图、ICFG 与上下文敏感
  ↓
指针/别名分析
  ↓
抽象解释、区间域、拓宽/收窄
  ↓
YASA-UAST 与符号解释
  ↓
污点、Call Graph、Checker 与规则工程
```

不要先背工具命令。每一步都应能回答四个问题：

1. 分析要判断什么程序性质？
2. 分析状态如何表示，序关系是什么？
3. 转移、汇聚和终止如何定义？
4. 结果允许误报还是漏报，保证范围是什么？

## 2. 六周核心计划

### 第 1 周：静态分析的边界与结果契约

**阅读**：课程总览、基础概念笔记。

**必须掌握**：

- 静态与动态分析的差别不是“是否看源码”，而是结论来自代码推理还是具体执行；
- 停机问题的对角论证；
- Rice 定理的“语义、非平凡、图灵完备”限定；
- 过近似 `R ⊆ A` 与欠近似 `A ⊆ R`；
- soundness、completeness、precision、may、must 不能机械互换；
- 抽象与搜索两种控制状态空间的思路。

**产出**：

- 不看笔记重写停机问题证明；
- 为“是否存在空指针路径”明确正例、误报、漏报；
- 给一个过近似和一个欠近似的最小程序例子。

### 第 2 周：中间表示

**阅读**：中间表示笔记。

**必须掌握**：

- 词法、语法、语义分析的输入和输出；
- CST/Parse Tree 与 AST 的信息差异；
- 基本块 leader 规则和 CFG 构造；
- 3AC 与临时变量；
- SSA 重命名、支配、支配边界、φ 的前驱选择语义；
- LLVM IR 中寄存器 SSA 与内存 `load/store` 的差别；
- YASA-UAST 为什么选择高层、统一但有损的 AST 表示。

**产出**：任选一个含 `if + while` 的 15 行程序，手工画 AST、划分基本块、画 CFG、转换 3AC 与 SSA。

### 第 3 周：经典数据流分析

**阅读**：数据流分析笔记前半。

**必须掌握**：

- 六要素：域、方向、转移、汇聚、边界、非边界初值；
- `OUT = GEN ∪ (IN − KILL)`；
- 可用表达式：forward + must；
- 到达定义：forward + may；
- 活跃变量：backward + may；
- 为什么“may 用并、must 用交”只在指定的幂集格口径下成立。

**产出**：

- 对同一 CFG 手算三种分析直至不动点；
- 写一个通用 Worklist 求解器；
- 用 bitset 实现至少两种 GEN/KILL 分析，并与手算表对照。

### 第 4 周：过程间、指针与抽象解释

**阅读**：数据流分析后半、指针与抽象解释笔记。

**必须掌握**：

- 直接调用、虚分派和函数指针对调用图的影响；
- ICFG 中 call、return、call-to-return 边；
- 不可实现路径与调用/返回匹配；
- 调用字符串和 `k` 限制的精度/终止权衡；
- points-to、may-alias、must-alias 的区别；
- Andersen 的 address/copy/load/store/GEP 约束；
- 具体域、抽象域、具体化、抽象转移；
- 区间不动点、widening 与 narrowing。

**产出**：

- 为一个含间接调用的小程序生成 Andersen 约束并手算 points-to 集；
- 对 `x=0; while(x<100) x=x+1` 手算普通迭代、拓宽和收窄；
- 构造一条上下文不敏感 ICFG 会误认为存在的不可实现路径。

### 第 5 周：YASA 入门与内部机制

**阅读/观看**：实验 1、实验 2，对应 Bilibili 视频和 YASA 官方文档。

**必须掌握**：

- `源代码 → 语言原生 AST → UAST → 预处理 → 从 entrypoint 模拟执行 → Checker → ResultManager/OutputStrategy`；
- UAST 的极大元选取、去语法糖、`meta` 保留语言特性、有损统一；
- 基础 Analyzer、语言 Analyzer、框架 Analyzer 的分层；
- 预处理为何先建立包、类、函数、变量与符号表；
- Scope、State、Callstack、分支状态和 Symbolic Value；
- AST 查询、Call Graph、污点规则的入口与输出。

**产出**：

- 运行 YASA 示例工程；
- 对一个文件输出 UAST；
- 生成并检查 Call Graph；
- 配置一条 Source→Sink 污点规则，记录命令、配置、输出和误报。

### 第 6 周：Checker 编写与课程项目

**阅读/观看**：实验 3、Checker 官方文档与源码。

**必须掌握**：

- Checker 是由 CheckerManager 管理的事件驱动插件；
- 生命周期与 `triggerAtStartOfAnalyze`、`triggerAtCompileUnit`、`triggerAtFunctionDefinition`、`triggerAtFunctionCallBefore/After`、`triggerAtAssignment`、`triggerAtEndOfAnalyze` 等 checkpoint；
- `analyzer/scope/node/state/info` 上下文分别提供什么；
- `checker-config`、`checker-pack-config`、`rule_config` 的职责差异；
- `ResultManager` 只收集 finding，`OutputStrategy` 决定 SARIF/JSON 等输出；
- 如何为一个 Web 框架识别 entrypoint、Source、Sink、Sanitizer 与回调关系。

**课程项目**：完成一个可复现 Checker。

最低要求：

1. 明确检测性质和保证范围；
2. 给出正例、反例、边界例及期望输出；
3. 选择正确 checkpoint；
4. 输出位置、证据链和规则 ID；
5. 在小型样例和真实仓库上运行；
6. 记录误报、漏报、耗时及原因；
7. 固定 YASA 版本、命令与配置。

可选题：课程演示的硬编码密码、Mux/其他框架入口识别，或空函数、超长文件、危险 API、缺失鉴权、命令注入。

## 3. 每章固定学习模板

每篇笔记都按以下顺序学习：

1. **动机**：为什么要计算该性质；
2. **形式化**：输入、输出、域、序关系；
3. **算法**：转移、汇聚、迭代、终止；
4. **保证**：sound/complete 与敏感性；
5. **例题**：至少手算一个循环或间接调用；
6. **工程映射**：在 YASA 中对应 UAST、Analyzer、Checker 或配置的哪一层；
7. **反例**：什么代码会损失精度；
8. **复现**：保存版本、配置、命令和结果。

## 4. 复习节奏

- **24 小时后**：不看资料写出定义、方程和算法步骤；
- **一周后**：重新手算一个未见过的 CFG；
- **两周后**：实现或修改一个分析；
- **一个月后**：用真实项目解释一次误报和一次漏报的根因。

复习卡不只问定义，还要问“为什么”。例如：

> 流不敏感分析一定不 sound 吗？

不是。soundness 是保证方向，flow sensitivity 是精度维度；流不敏感分析可以 sound，但通常更保守。

## 5. 学习完成标准

达到以下标准才算真正掌握：

- 能从自然语言需求推出分析域和数据流方程；
- 能手工求循环不动点并说明终止；
- 能辨认不可靠结论与单纯不精确结论；
- 能解释调用图/别名误差如何污染后续污点分析；
- 能读懂一个 YASA Checker 的触发点、状态访问与输出路径；
- 能用最小反例定位错误来自前端、IR、模型、求解器、规则还是报告层。

## 6. 系列之外的后续方向

完成本系列后，再扩展：程序语义与 Galois 连接、SSA 稀疏分析、Steensgaard、对象敏感、IFDS/IDE、程序切片、符号执行、模型检查、Datalog/CodeQL。它们不是当前 8 篇文章已经完整讲授的内容，应作为第二阶段，而不是混入原系列冒充覆盖。
