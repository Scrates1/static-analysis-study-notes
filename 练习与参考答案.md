# 练习与参考答案

> 建议先独立完成，再展开参考答案。答案给出判断依据，不只给结论。

## 一、基础概念

### 1. 不可判定性

判断下列说法是否正确，并解释：

1. 停机问题不可判定，所以静态分析没有意义。
2. 对任意 C 程序判断源文件是否出现 `malloc` 受 Rice 定理约束。
3. 对循环次数有静态上界的有限状态程序，某些一般情形不可判定的问题可以变为可判定。

<details><summary>参考答案</summary>

1. 错。不可判定排除的是对所有程序都终止且零误差的通用判定器；近似、受限语言、返回 unknown、预算内搜索仍然有用。
2. 错。这是源代码的语法性质，不是只依赖所计算函数的外延语义性质。
3. 对。限制程序模型或状态空间后可以穷举或做有限不动点求解。

</details>

### 2. 过近似与欠近似

真实可达状态为 `R={1,2,3}`。分析器 A 返回 `{1,2,3,4}`，分析器 B 返回 `{1,3}`。

1. 谁是过近似，谁是欠近似？
2. 用 A 未发现坏状态 `4`，能证明安全吗？
3. 用 B 发现坏状态 `3`，能证明存在真实反例吗？

<details><summary>参考答案</summary>

1. `R⊆A`，A 是过近似；`B⊆R`，B 是欠近似。
2. 题设中 A 实际包含 4，所以前提不成立。一般地，若 sound 过近似中没有坏状态，则真实状态也没有；发现坏状态可能是假阳性。
3. 若 B 的每个状态都对应真实执行，则可以；欠近似没找到不能证明安全。

</details>

### 3. Soundness 与 Completeness

为什么在说“工具是 sound 的”之前必须定义正例？分别以“报告漏洞”和“证明安全”为正例写出 TP、FP、FN。

<details><summary>参考答案</summary>

若正例是“程序有漏洞”：TP=程序有漏洞且工具报告漏洞；FP=程序无漏洞但工具报告漏洞；FN=程序有漏洞但工具未报告。安全分析文献中所谓“不漏掉任何可能错误”的 soundness，对应这一正例定义下没有 FN。

若正例是“程序安全”：TP=程序安全且工具证明安全；FP=程序不安全但工具错误地证明安全；FN=程序安全但工具未能证明安全。此时安全证明的 soundness 要求没有 FP。

因此，在讨论 soundness、completeness、误报或漏报前，必须先说明查询、正例及工具输出的含义。

</details>

## 二、中间表示

### 4. AST、CFG 与基本块

对下面程序画出简化 AST 和 CFG，并标出基本块 leader：

```c
x = 0;
while (x < n) {
    if (a[x] == 0) break;
    x = x + 1;
}
return x;
```

<details><summary>参考答案要点</summary>

- AST 根可为函数体/语句序列，`while` 含条件和 body，body 含 `if-break` 与赋值；
- leader 的标准规则是：过程首指令、任一跳转目标，以及任一条件/无条件跳转指令紧后的指令。本题对应的块首可取初始化语句、`while` 条件、`if` 条件、自增语句和 `return` 语句；
- CFG 至少有初始化块、while 条件块、if 条件块、自增块、return 块；
- `while false` 和 `break` 都到 return，自增回到 while 条件。

</details>

### 5. SSA

把下列程序变为寄存器 SSA：

```text
x = 0
if c:
    x = 1
else:
    x = 2
y = x + 3
```

<details><summary>参考答案</summary>

```text
x0 = 0
if c goto L1 else L2
L1: x1 = 1; goto L3
L2: x2 = 2; goto L3
L3: x3 = φ(L1:x1, L2:x2)
    y0 = x3 + 3
```

`φ` 按实际到达的前驱取值，不是在运行时任意选一个值。

</details>

### 6. 内存为什么不能只靠普通 SSA

解释为什么 `*p = 1; x = *q` 即使每个寄存器只定义一次，也仍需别名/Memory SSA 等机制。

<details><summary>参考答案</summary>

`p` 和 `q` 的寄存器版本唯一并不说明它们指向的内存对象是否相同。若 may-alias，store 可能定义后续 load 读取的内存；普通标量 SSA 没有把该内存定义—使用关系显式化。

</details>

## 三、过程内数据流

### 7. 识别六要素

为可用表达式、到达定义、活跃变量分别填出方向、汇聚、边界和常见初值。

<details><summary>参考答案</summary>

| 分析 | 方向 | 汇聚 | 边界 | 非边界初值 |
|---|---|---|---|---|
| 可用表达式 | forward | 交 | 入口空集 | 表达式全集 |
| 到达定义 | forward | 并 | 入口空集 | 空集 |
| 活跃变量 | backward | 并 | 出口空集 | 空集 |

都必须再给出各自 GEN/KILL/USE/DEF 转移；表格成立于本课程采用的幂集格口径。

</details>

### 8. `a = a - 1` 会生成 `a-1` 吗

在标准基本块出口处的 available-expressions 分析中，语句 `a=a-1` 是否 GEN 表达式 `a-1`？

<details><summary>参考答案</summary>

不生成。右侧计算使用旧 `a`，语句后 `a` 已被重新定义，所以刚算出的值不代表出口状态中以新 `a` 求得的 `a-1`。可写作 `GEN=EvalExpr(rhs)−KILL`。

</details>

### 9. 活跃变量手算

```text
1: x = a + b
2: y = x + 1
3: if c goto 5
4: y = 0
5: return y
```

求每行前的活跃变量集合。

<details><summary>参考答案</summary>

从后向前：

- `IN5={y}`；
- `IN4=∅`（`y` 被定义，出口需 y）；
- `IN3={c,y}`，因为两个后继的 IN 并为 `{y}`，条件使用 c；
- `IN2={x,c}`，定义 y、使用 x；
- `IN1={a,b,c}`，定义 x、使用 a/b。

</details>

### 10. Worklist

为什么节点状态变化后应把受影响的邻居入队，而不是无条件把自己入队？

<details><summary>参考答案</summary>

forward 分析中，`OUT[n]` 变化会改变后继的 `IN`；backward 分析中，`IN[n]` 变化会改变前驱的 `OUT`。把依赖者入队符合方程依赖；只把自己入队可能永远不传播到消费者。

</details>

## 四、过程间分析

### 11. 不可实现路径

同一调用者有两个调用点 `c1`、`c2` 都调用 `f`。为什么上下文不敏感 ICFG 可能产生 `c1→f→return-to-c2` 的路径？如何排除？

<details><summary>参考答案</summary>

callee exit 同时连接两个 return site，普通图可达性不记录“从哪个调用点进入”。将状态扩展为 `(node, call-stack)`：call 边压入匹配返回点，return 边只在栈顶匹配时弹出，即可排除不匹配路径。

</details>

### 12. `k`-call-string 的代价

增大 call-string 上下文长度 `k` 通常对精度、时间和内存有什么影响？`k=0` 表示什么？为什么截断后的 call string 不能直接当作完整运行栈处理返回？

<details><summary>参考答案</summary>

`k` 增大时可以区分更多调用上下文，通常减少误合并，但状态数、时间和内存开销上升。`k=0` 不保留调用上下文，即上下文不敏感；有限 `k` 使有限程序中的调用上下文数量保持有限。截断后的 call string 已丢失更早的调用前缀，不能继续按完整栈做精确 pop；返回应通过 continuation 表映射回相应 caller，或在显式有限栈中使用 overflow/unknown-prefix 等保守规则。它与经典 k-CFA 的思想相关，但不应直接等同。

</details>

## 五、指针与抽象解释

### 13. Points-to 与 Alias

若 `pts(p)={o1,o2}`，`pts(q)={o2,o3}`，可以得到哪些 may/must alias 结论？

<details><summary>参考答案</summary>

两个 points-to 集的交集包含 `o2`，因此该集合分析会报告 may-alias，不能证明 no-alias。若 points-to 集只是 sound 过近似，这并不保证存在一次真实执行使二者同时指向 `o2`。当前结果也不能推出 must-alias。只有当 `pts(p)=pts(q)={o}`，且 `o` 代表唯一具体位置、两者在所有相关执行中同时有效，并已排除 null、生命周期和汇总对象歧义时，才能据此推出 must-alias。

</details>

### 14. Andersen 约束

为下列语句写包含式约束：

```c
p = &a;
q = p;
r = *q;
*s = r;
t = &p->f;
```

<details><summary>参考答案</summary>

- `p=&a`：`{a}⊆pts(p)`；
- `q=p`：`pts(p)⊆pts(q)`；
- `r=*q`：对每个 `o∈pts(q)`，`pts(o)⊆pts(r)`；
- `*s=r`：对每个 `o∈pts(s)`，`pts(r)⊆pts(o)`；
- `t=&p->f`：对每个 `o∈pts(p)`，`{o.f}⊆pts(t)`。

</details>

### 15. 区间不动点

对 `a=0; while(a<10) a++;`，不使用 widening 时循环头和循环体后的精确区间是什么？

<details><summary>参考答案</summary>

循环头（判断前）为 `[0,10]`；真分支约束后为 `[0,9]`，加一得到循环体后 `[1,10]`；假分支 `a≥10` 与头区间相交得到出口 `[10,10]`。

</details>

### 16. Widening/Narrowing

区间 widening 由 `[0,0]` 遇到 `[0,1]` 得到什么？为何随后 narrowing 可恢复 `[0,10]`？

<details><summary>参考答案</summary>

上界向外增长，经典 widening 将其跳到 `+∞`，得到 `[0,+∞]`。重新应用带分支约束的原转移会给出候选 `[0,10]`；经典 narrowing 允许把原来为无穷的界替换为候选有限界，因此收缩到 `[0,10]`。

</details>

## 六、YASA 实验

### 17. 命令选择

分别说明以下目标需要的核心选项：

1. 导出单文件 UAST；
2. 导出工程 Call Graph；
3. 运行污点规则。

<details><summary>参考答案</summary>

1. `--sourcePath`、`--dumpAST`、`--language`；Go/Python 还需对应 `--uastSDKPath`，可用 `--report` 指定文件。
2. `--sourcePath`、`--dumpAllCG`、语言或 Analyzer、可选 `--cgAlgo CHA|DEFAULT` 与 `--report`。
3. `--sourcePath`、`--checkerPackIds`/`--checkerIds`、`--ruleConfigFile`、语言或 Analyzer、输出目录；Go/Python 通常还需 UAST SDK。

实际参数以固定版本的 `--help` 为准。

</details>

### 18. 配置层次

解释 `checker-config.json`、`checker-pack-config.json`、`rule_config.json` 的区别。

<details><summary>参考答案</summary>

- checker-config：Checker ID 到实现路径及描述的注册表；
- checker-pack-config：把多个 Checker ID 组合为可启用策略包；
- rule_config：某些 Checker 的用户数据，如 source/sink/sanitizer/entrypoint，并用 `checkerIds` 关联生效对象。

</details>

### 19. Checker checkpoint

要检测“源文件超过 200 行”和“危险函数调用的第 0 个参数是否带污点”，分别优先选择什么触发点？

<details><summary>参考答案</summary>

前者可在 `triggerAtCompileUnit` 根据 `CompileUnit.loc` 检查文件行数；后者通常在 `triggerAtFunctionCallBefore` 获取 callee、参数符号值和当前状态。也可在文件结束或分析结束统一报告，但检测所需信息应在最贴近语义的 checkpoint 收集。

</details>

### 20. 设计一个框架适配 Checker

为某 Web 框架列出最小建模清单。

<details><summary>参考答案要点</summary>

- 路由注册 API 与 handler 参数位置；
- handler/回调到函数定义的解析；
- entrypoint 采集；
- request 参数、header、body 等 Source；
- SQL、命令执行、文件/网络等 Sink；
- 验证、编码、白名单等 Sanitizer 语义；
- 框架隐式调用和生命周期；
- 正例、反例、异步/装饰器/别名边界例；
- 输出证据链与误报/漏报记录。

</details>

### 21. 复现硬编码密码 Checker 的设计

不依赖具体源码，按课程中的五步法说明如何实现“变量声明中存在硬编码密码”检测，并指出至少两个误报来源。

<details><summary>参考答案要点</summary>

1. **功能**：变量名含 `password`、`pwd` 等关键词，初始化值为字符串字面量，并符合弱密码/纯数字/纯字母等可配置模式；
2. **触发点**：在课程目标版本的 `triggerAtPreDeclaration` 检查变量声明；在 `triggerAtEndOfAnalyze` 汇总并创建 findings；
3. **触发方法**：读取声明节点的变量名、初始化节点类型、字面量值和源码位置；不要把任意表达式误当成字面量；
4. **输出**：通过 ResultManager 写入规则 ID、说明、位置、变量名和严重度；如确有定制需求，再实现 `HardcodedPasswordOutputStrategy` 一类输出策略；
5. **注册**：把 Checker 加入 `checker-config.json`，需要组合时再加入 CheckerPack；具体 hook 和字段必须以固定版本为准。

误报来源包括：示例/测试代码、占位值、被覆盖或仅用于文档的变量、名为 password 但不是凭据的字符串、加密后的常量。反例还应覆盖动态配置、环境变量、秘密管理服务和非字符串初值。

</details>

## 七、综合项目评分表

| 维度 | 0 分 | 1 分 | 2 分 |
|---|---|---|---|
| 性质定义 | 模糊 | 有目标无量词 | 明确正例、保证与边界 |
| IR/模型 | 未说明 | 能运行 | 解释选择及信息损失 |
| 算法 | 无 | 复用默认 | 方程/触发点/终止自洽 |
| 测试 | 仅一个样例 | 正反例 | 含边界、回归与真实项目 |
| 结果 | 截图 | 可读报告 | 含证据链、位置、规则 ID |
| 评估 | 无 | 报数量 | 分析误报、漏报、性能根因 |
| 复现 | 不可复现 | 有命令 | 固定版本、配置、命令、输入 |

总分 14；达到 11 分以上，且“性质定义”“复现”均为 2 分，才算完成课程项目。
